#include"monty.h"

/**
 * f_comment - Treats the line as a comment (no action taken).
 * @head: Pointer to the stack head.
 * @counter: Line number.
 * Return: No return value.
 */
void f_comment(stack_t **stack, unsigned int counter)
{
	/*This function is used to treat the line as a comment.*/
	/*No action is taken, and the line is ignored.*/
	(void)stack;/*Unused parameter to avoid compiler warnings*/
	(void)counter;/*Unused parameter to avoid compiler warnings*/
}
