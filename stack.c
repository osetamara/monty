#include "monty.h"
/**
 * f_stack - prints the top
 * @stack: stack 
 * @counter: line_number
 * Return: no return
*/
void f_stack(stack_t **stack, unsigned int counter)
{
	(void)stack;
	(void)counter;
	bus.lifi = 0;
}
